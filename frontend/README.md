# Frontend (PostFlow)

Фронтенд — React + Vite. Основная документация и команды — в корневом `README.md`.

Полезные команды (из этой папки):

```bash
npm run dev
npm run build
npm run lint
npm run typecheck
```
